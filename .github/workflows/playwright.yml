name: Playwright Tests

on:
  push:
    branches:
      - main
      - demoqa-tests
  pull_request:
    branches:
      - main
      - demoqa-tests
  schedule:
    - cron: '50 1 * * *'

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        browser: [chrome, firefox]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install dependencies
      run: npm ci

    - name: Install browsers for Playwright
      run: npx playwright install

    - name: Run Сucumber tests
      run: |
        sudo apt-get install xvfb
        xvfb-run --auto-servernum --server-args="-screen 0 1920x1080x24" npm run test:cucumber

    - name: Run Playwright tests
      run: |
        if [ "${{ matrix.browser }}" == "chrome" ]; then
          xvfb-run --auto-servernum --server-args="-screen 0 1920x1080x24" npm run test:chrome
        elif [ "${{ matrix.browser }}" == "firefox" ]; then
          xvfb-run --auto-servernum --server-args="-screen 0 1920x1080x24" npm run test:firefox
        fi

    - name: Upload reports
      uses: actions/upload-artifact@v4
      with:
        name: playwright-report-${{ matrix.browser }}
        path: reports
